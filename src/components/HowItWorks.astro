---
const steps = [
  {
    number: '1',
    title: 'Envía un mensaje',
    description: 'uber 6500, almuerzo 8000 o una foto de la boleta',
    icon: '💬',
  },
  {
    number: '2',
    title: 'Gastify lo clasifica',
    description: 'Categoriza automáticamente: 🚗 Transporte — $6.500',
    icon: '🤖',
  },
  {
    number: '3',
    title: 'Ve tu resumen',
    description: 'Resúmenes diarios, semanales y mensuales con IA',
    icon: '📊',
  },
];
---

<section class="py-24">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <div class="text-center fade-up">
      <h2 class="text-3xl font-bold text-white sm:text-4xl">Cómo funciona</h2>
      <p class="mt-4 text-lg text-gray-400">Tres pasos. Cero complicaciones.</p>
    </div>

    <div class="mt-16 grid gap-8 sm:grid-cols-3">
      {steps.map((step, i) => (
        <div class="fade-up group relative rounded-2xl border border-border bg-surface p-8 text-center transition-all hover:border-teal/30 hover:shadow-lg hover:shadow-teal/5" style={`transition-delay: ${i * 100}ms`}>
          <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl bg-teal/10 text-3xl">
            {step.icon}
          </div>
          <div class="mt-2 text-sm font-semibold text-teal">Paso {step.number}</div>
          <h3 class="mt-3 text-xl font-bold text-white">{step.title}</h3>
          <p class="mt-2 text-gray-400">{step.description}</p>
        </div>
      ))}
    </div>
  </div>
</section>
